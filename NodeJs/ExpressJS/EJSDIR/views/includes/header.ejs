<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>instaProfile-DBMS</title>
    <style>
        body {
            background-color: #fafafa;
            /* halka grey background */
            font-family: Arial, sans-serif;
            /* clean font */
            margin: 0;
            padding: 0;
        }

        h1 {
            color: #333;
            /* dark grey for headings */
            text-align: center;
            margin-top: 20px;
        }

        button {
            background-color: #3897f0;
            /* Instagram blue */
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 4px;
        }

        div {
            text-align: center;
            margin: 20px 0;
        }
        img {
            display: block;
            margin: 20px auto;
            border-radius: 8px;
        }

        p {
            text-align: center;
            color: #555;
        }
        footer {
            background-color: #fff;
            border-top: 1px solid #dbdbdb;
            padding: 20px;
            text-align: center;
            margin-top: 40px;
        }
    </style>
</head>